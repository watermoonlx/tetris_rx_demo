import { PieceBase } from '../piece/PieceBase';
import { } from '../common/PieceType';

export class MainWindow {
    public static size = {
        width: 12,
        height: 24
    };

    public static initialPosition=

    constructor(private piece: PieceBase) {

    }

    paint(): void {

    }
}
